name = "justjavac/itoa"

version = "0.2.4"

readme = "README.mbt.md"

repository = "https://github.com/justjavac/moonbit-itoa"

license = "MIT"

keywords = [ "itoa", "conversion", "integer", "bytes", "utility" ]

description = "Fast decimal formatting for signed and unsigned integers"

options(
  source: "src",
)
